
class ChatSetting {
    String chatImg;
    String title;
    String intro;
    bool isPublic;
    int inviteRange;
    int mode;

    ChatSetting({this.chatImg ,this.title, this.intro, this.isPublic, this.inviteRange, this.mode});
}